[package]
name = "rack-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow.workspace = true
clap = { version = "4", features = ["derive"] }
rack-core = { path = "../core" }
rack-hooks = { path = "../hooks" }
rack-services = { path = "../services" }
